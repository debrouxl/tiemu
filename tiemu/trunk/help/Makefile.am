helpdir = $(docdir)/help
dist_help_DATA = *.html *.png *.jpg

uninstall:
	rm -f $(docdir)/help/*.html
	rm -f $(docdir)/help/*.png
	rm -f $(docdir)/help/*.jpg


dos2unix:
	dos2unix
	lowercase
	uppercase
