/*
 * DO NOT EDIT THIS FILE - it is generated by Glade.
 */

#ifdef HAVE_CONFIG_H
#  include <config.h>
#endif

#include <sys/types.h>
#include <sys/stat.h>
//#include <unistd.h>
#include <string.h>

#include <gdk/gdkkeysyms.h>
#include <gtk/gtk.h>

#include "state_cb.h"
#include "state_dbox.h"
#include "support.h"

GtkWidget*
create_fs_load_state (void)
{
  GtkWidget *fs_load_state;
  GtkWidget *ok_button1;
  GtkWidget *cancel_button1;
  GtkAccelGroup *accel_group;

  accel_group = gtk_accel_group_new ();

  fs_load_state = gtk_file_selection_new (_("Select File"));
  gtk_widget_set_name (fs_load_state, "fs_load_state");
  gtk_object_set_data (GTK_OBJECT (fs_load_state), "fs_load_state", fs_load_state);
  gtk_container_set_border_width (GTK_CONTAINER (fs_load_state), 10);

  ok_button1 = GTK_FILE_SELECTION (fs_load_state)->ok_button;
  gtk_widget_set_name (ok_button1, "ok_button1");
  gtk_object_set_data (GTK_OBJECT (fs_load_state), "ok_button1", ok_button1);
  gtk_widget_show (ok_button1);
  GTK_WIDGET_SET_FLAGS (ok_button1, GTK_CAN_DEFAULT);
  gtk_widget_add_accelerator (ok_button1, "clicked", accel_group,
                              GDK_Return, 0,
                              GTK_ACCEL_VISIBLE);

  cancel_button1 = GTK_FILE_SELECTION (fs_load_state)->cancel_button;
  gtk_widget_set_name (cancel_button1, "cancel_button1");
  gtk_object_set_data (GTK_OBJECT (fs_load_state), "cancel_button1", cancel_button1);
  gtk_widget_show (cancel_button1);
  GTK_WIDGET_SET_FLAGS (cancel_button1, GTK_CAN_DEFAULT);
  gtk_widget_add_accelerator (cancel_button1, "clicked", accel_group,
                              GDK_Escape, 0,
                              GTK_ACCEL_VISIBLE);

  gtk_signal_connect (GTK_OBJECT (fs_load_state), "destroy",
                      GTK_SIGNAL_FUNC (on_fs_load_state_destroy),
                      NULL);
  gtk_signal_connect (GTK_OBJECT (ok_button1), "clicked",
                      GTK_SIGNAL_FUNC (load_state_on_ok_button1_clicked),
                      fs_load_state);
  gtk_signal_connect (GTK_OBJECT (cancel_button1), "clicked",
                      GTK_SIGNAL_FUNC (load_state_on_cancel_button1_clicked),
                      fs_load_state);

  gtk_window_add_accel_group (GTK_WINDOW (fs_load_state), accel_group);

  return fs_load_state;
}

GtkWidget*
create_fs_save_state (void)
{
  GtkWidget *fs_save_state;
  GtkWidget *ok_button2;
  GtkWidget *cancel_button2;
  GtkAccelGroup *accel_group;

  accel_group = gtk_accel_group_new ();

  fs_save_state = gtk_file_selection_new (_("Select File"));
  gtk_widget_set_name (fs_save_state, "fs_save_state");
  gtk_object_set_data (GTK_OBJECT (fs_save_state), "fs_save_state", fs_save_state);
  gtk_container_set_border_width (GTK_CONTAINER (fs_save_state), 10);

  ok_button2 = GTK_FILE_SELECTION (fs_save_state)->ok_button;
  gtk_widget_set_name (ok_button2, "ok_button2");
  gtk_object_set_data (GTK_OBJECT (fs_save_state), "ok_button2", ok_button2);
  gtk_widget_show (ok_button2);
  GTK_WIDGET_SET_FLAGS (ok_button2, GTK_CAN_DEFAULT);
  gtk_widget_add_accelerator (ok_button2, "clicked", accel_group,
                              GDK_Return, 0,
                              GTK_ACCEL_VISIBLE);

  cancel_button2 = GTK_FILE_SELECTION (fs_save_state)->cancel_button;
  gtk_widget_set_name (cancel_button2, "cancel_button2");
  gtk_object_set_data (GTK_OBJECT (fs_save_state), "cancel_button2", cancel_button2);
  gtk_widget_show (cancel_button2);
  GTK_WIDGET_SET_FLAGS (cancel_button2, GTK_CAN_DEFAULT);
  gtk_widget_add_accelerator (cancel_button2, "clicked", accel_group,
                              GDK_Escape, 0,
                              GTK_ACCEL_VISIBLE);

  gtk_signal_connect (GTK_OBJECT (fs_save_state), "destroy",
                      GTK_SIGNAL_FUNC (on_fs_save_state_destroy),
                      NULL);
  gtk_signal_connect (GTK_OBJECT (ok_button2), "clicked",
                      GTK_SIGNAL_FUNC (save_state_on_ok_button2_clicked),
                      fs_save_state);
  gtk_signal_connect (GTK_OBJECT (cancel_button2), "clicked",
                      GTK_SIGNAL_FUNC (save_state_on_cancel_button2_pressed),
                      fs_save_state);

  gtk_window_add_accel_group (GTK_WINDOW (fs_save_state), accel_group);

  return fs_save_state;
}

