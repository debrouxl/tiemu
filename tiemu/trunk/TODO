Planned features:
-----------------

* use the contrast field of skin
* tracing/logging of mem access or others

YaroNet forum:
--------------

* = to do
x = done

* multi-fenêtre polluant => debugger style VTi (ou utiliser des fenetres dockables)
* le toolkit GTK et ses dépendances
x utiliser la même police que la fenêtre register dans les autres fenêtres
* Si on appuie sur F11 lors du chargement (preloading débugger), au tout début ça fait crasher TiEmu, et vers la fin ça affiche les fenêtres du débogueur, mais elles sont toutes grisées et la calc semble fonctionner normalement (mais dur à reproduire ça, ça doit être à un moment très précis).
* Si on appuie sur F12 lors du chargement (preloading debugger), le menu des ROMs se présente, mais on peut choisir ce qu'on veut, ça ne change strictement rien, et le modèle de calc par défaut se charge. Le clignotement du curseur dans HOME est alors anormal (trop rapide), mais régulier.
x ça serait possible que les deux touches Ctrl soient reconnues comme <> svp?
x çà serait possible que la touche ENTER du pavé numérique soit reconnue comme ENTER sur ti89?

post 297 (Pollux):
<<
- débuggeur 
 ? F5 sur un breakpoint ne fait que relancer le debugger sans rien exécuter 
 * F8 foire qd la fonction skippée contient un breakpoint (freeze jusqu'à ce qu'on fasse F11) 

- fenêtre code 
  x je trouve pas les 0 inutiles très lisible : rol.l #$00000007,d0 :/ (VTI, lui, supprime toujours les 0 de tête) 
  x c'est un peu dommage de pas pouvoir distinguer move.l de moveq, 
  - c'est un peu dommage de pas pouvoir distinguer addq/subq
  - movem.l #$masque_incompréhensible, il vaudrait mieux les registres comme dans VTI 
  x movem.l prend 2 octets de trop donc il "mange" l'instruction suivante 
  - pour les adressages indexés : 
   x il vaudrait mieux mettre la constante avant le registre, c'est mieux d'avoir lea ($42,a3),a3 pour 
     voir du premier coup d'oeil que a3 et a3 c'est bien le même registre 
   x (a3,d0.l*1) : le *1 ne sert à rien, surtout sur 68000 
   x VTI utilise lea (-$c,a3),a3 au lieu de lea ($fff4,a3),a3, c'est plus lisible 
  x il y a "trap .l" au lieu de "trap #$C" 
  x rts.l  (heu!??)
  x il y a "bt<tab>.b" au lieu de "bt.b<tab>" (et sinon bra serait plus joli que bt)
 x impossible de savoir si l'instruction sélectionnée est l'instruction courante
 x les "= bidule" pourraient peut-être être colorés, je ne sais pas si c'est possible
 x les conventions ne sont pas très cohérentes, un coup c'est [42a1], un coup c'est [$42a1], un coup c'est = $42a1) 

- fenêtre registres 
 x les "D3=" sont sélectionnables mais
 x le double clic ne sélectionne qu'un groupe de chiffres ou un groupe de lettres mais pas la valeur hexa entière 

- fenêtre stack 
 ? +4 / +2 / 0 / +2 / +4 / ... au lieu de -4 / -2 / 0 / +2 / +4 / ... 

- fenêtre memory 
 x scroll un peu lent 
 x pas de raccourcis clavier: Ajouter/Suprimer des onglets, et se déplacer d'onglet en onglet. Pour pouvoir naviguer sans toucher à la souris quoi.

x idée d'amélioration : ajouter via le clic droit "ouvrir cette adresse dans la fenêtre memory" 
  (sur toutes les adresses, aussi bien dans code/disassembly/memory/stack [bon ok pour les 2 derniers c'est un peu plus 
  compliqué puisque ça marche par octet ou par mot, mais au moins pour les deux premiers ce serait cool])
  >>